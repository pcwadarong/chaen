/**
 * 홈 히어로 이미지 뷰어와 액자 선택 UI에서 공통으로 사용하는 이미지 항목입니다.
 *
 * `Readonly` 계약이라 생성 후 필드를 변경하지 않고 그대로 소비해야 합니다.
 * `alt`는 뷰어 접근성 이름과 썸네일 설명에 사용되며, 예를 들어 파일명이나 사진 설명을 넣습니다.
 * `src`는 브라우저가 바로 로드할 수 있는 절대/상대 URL 문자열이어야 하며, 빈 문자열이면 렌더링 시
 * 이미지가 보이지 않거나 선택 복원 로직이 깨질 수 있으므로 호출부에서 미리 걸러내야 합니다.
 */
export type HomeHeroImageViewerItem = Readonly<{
  alt: string;
  src: string;
}>;
