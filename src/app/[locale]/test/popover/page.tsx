import { PopoverE2eFixture } from '@/shared/ui/popover/popover-e2e-fixture';

/**
 * Popover의 브라우저 계약을 검증하기 위한 locale fixture 페이지입니다.
 */
const PopoverTestPage = () => <PopoverE2eFixture />;

export default PopoverTestPage;
